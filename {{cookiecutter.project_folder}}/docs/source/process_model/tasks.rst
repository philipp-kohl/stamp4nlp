Tasks
=====

One task forms a step in the process model. It can be a automatic executed task or a user executed task.
The user needs specific skills for the task execution, which are constituted by roles.
Some tasks require inputs and creates outputs: artifacts.

The following tasks are part of the NLP Process model.

.. toctree::
   :maxdepth: 1
   :glob:

   tasks/*